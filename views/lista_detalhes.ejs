<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista: <%= nomeLista %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/styles/lista_detalhes.css">
</head>
<body>

    <header>
        <div>
            <span style="color: #f5c518; text-transform: uppercase; font-size: 0.8rem; font-weight: bold; letter-spacing: 1px;">A tua lista personalizada</span>
            <h1><%= nomeLista %></h1>
        </div>
        <a href="/perfil" class="btn-back"><i class="fa-solid fa-arrow-left"></i> Voltar ao Perfil</a>
    </header>

    <% if (filmes && filmes.length > 0) { %>
        <div class="grid">
            <% filmes.forEach(f => { %>
                <div class="card">
                    <a href="/movie/<%= f.tmdb_id %>" style="text-decoration: none;">
                        <img src="<%= imageBaseUrl + f.poster_url %>" alt="<%= f.nome %>">
                        <div class="info">
                            <h4><%= f.nome %></h4>
                        </div>
                    </a>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <div class="empty-state">
            <i class="fa-solid fa-film"></i>
            <h2>Esta lista ainda está vazia.</h2>
            <p>Navega pelo catálogo e adiciona filmes clicando no botão "+"!</p>
            <br>
            <a href="/" style="color: #f5c518; text-decoration: underline;">Explorar Filmes Agora</a>
        </div>
    <% } %>

</body>
</html>